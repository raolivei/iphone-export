apiVersion: apps/v1
kind: Deployment
metadata:
  name: iphone-export-postgres
  namespace: iphone-export
  labels:
    app: iphone-export
    component: postgres
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: iphone-export
      component: postgres
  template:
    metadata:
      labels:
        app: iphone-export
        component: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
              name: postgres
          env:
            - name: POSTGRES_DB
              value: "iphone_export"
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: iphone-export-secrets
                  key: postgres-user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: iphone-export-secrets
                  key: postgres-password
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - postgres
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - postgres
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: iphone-export-postgres-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: iphone-export-postgres
  namespace: iphone-export
  labels:
    app: iphone-export
    component: postgres
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  selector:
    app: iphone-export
    component: postgres





